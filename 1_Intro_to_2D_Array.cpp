#include< iostream>
using namespace std;

int main(){
    int arr[4][3];

    // taking input users from row wise input
    for (int row=0; row <4;row++){
        for (int col=0; col<3; col++){
            cin >> arr[row][col];

        }
    }
    // taking input =col wise input
    for (int col=0; col<4; col++){
        for (int row =0 ; col <3; col++)
    }
}
