#include<bits/stdc++.h>
using namespace std;

int main(){
    int n;
     cin>> n;
     int arr[n];  //it is a bad pratice because it uses stack and it has a less memory space and if the array is large it will crash
     return 0;

}